# Dataset Note

The full raw fragrance dataset is not included in this repository because the file exceeds GitHub's file size limit.

A sample version of the raw dataset is included for preview purposes:

- `raw_fragrance_dataset_sample.csv`

The SQL cleaning process was performed on the full dataset locally in MySQL. The final cleaned dataset contained 69,948 fragrance records.
